# L2 Send Guard — copy to .env (never commit .env)

# Listen
L2SG_HOST=127.0.0.1
L2SG_PORT=8545

# Guard policy: open = fail-open on uncertain (default); strict = abort if sim missing/unknown/low-confidence
GUARD_MODE=open
# Alias: L2SG_GUARD_MODE=open|strict
# Legacy (used only when GUARD_MODE unset): L2SG_FAIL_OPEN=true

# Comma-separated chain keys (≥2 for multi-ecosystem demo)
# Options: arb-sepolia, op-sepolia, base-sepolia
L2SG_CHAINS=arb-sepolia,op-sepolia,base-sepolia
L2SG_DEFAULT_CHAIN=arb-sepolia

# Optional: override public testnet RPCs (no API keys required for smoke demo)
# L2SG_RPC_ARB_SEPOLIA=https://sepolia-rollup.arbitrum.io/rpc
# L2SG_RPC_BASE_SEPOLIA=https://sepolia.base.org
# L2SG_RPC_OP_SEPOLIA=https://sepolia.optimism.io

# Tip: point these at your existing Alchemy/QuickNode/eRPC URLs in production.
# This middleware sits IN FRONT of them — it is not a replacement RPC cloud.

# Optional: alternate RPC used ONLY for simulation when primary eth_call is uncertain
# (send forwarding always uses L2SG_RPC_*). Useful if primary public RPC lacks
# eth_simulateV1 (e.g. Base Sepolia -32602) but you have another node that supports it.
# L2SG_RPC_FALLBACK_ARB_SEPOLIA=
# L2SG_RPC_FALLBACK_BASE_SEPOLIA=
# L2SG_RPC_FALLBACK_OP_SEPOLIA=

# Delta A burner (NEVER commit real values — use local .env only)
# L2SG_BURNER_KEY=0x...
# L2SG_BURNER_ADDRESS=0x...

# --- Layer 2 address/spend policy (OPTIONAL — default OFF) ---
# Answers "should this address get money at all?" beside Layer 1 simulation.
# When enabled, denials are definite stops (-32083), never fail-open.
# L2SG_POLICY_ENABLED=false
# L2SG_POLICY_FILE=./policy.example.json
# L2SG_POLICY_ALLOWLIST=0x1111111111111111111111111111111111111111,0x2222222222222222222222222222222222222222
# L2SG_POLICY_GLOBAL_MAX_WEI=100000000000000000
# L2SG_POLICY_ALLOW_CREATE=false
# L2SG_POLICY_ALLOW_ANY=false
# L2SG_POLICY_NOTIFY_URL=
# L2SG_POLICY_ERC20_RECIPIENT_CHECK=true
