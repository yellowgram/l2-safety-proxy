name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-and-demo:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install
        run: npm ci

      - name: Unit + integration tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Offline abort + fail-open demo
        run: npm run demo:offline
